@model FutbotecaApi.Models.Seguimiento

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Seguimiento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="SeguidorId" class="control-label"></label>
                <select asp-for="SeguidorId" class="form-control" asp-items="ViewBag.SeguidorId"></select>
                <span asp-validation-for="SeguidorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SeguidoId" class="control-label"></label>
                <select asp-for="SeguidoId" class="form-control" asp-items="ViewBag.SeguidoId"></select>
                <span asp-validation-for="SeguidoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
